# 镜像与 Docusaurus 版本配置
# 升级时只需修改 DOCUSAURUS_VERSION 为目标版本，然后执行：
#   docker compose pull && docker compose up -d
DOCUSAURUS_VERSION=3.10.2
IMAGE_NAME=docusaurus-site

# 基础镜像源（国内 DaoCloud 镜像），可选：docker.1ms.run、docker.xuanyuan.me
BASE_REGISTRY=docker.m.daocloud.io

DOCKERHUB_REPO=wkao/docusaurus-site