- 配置文件使用 `// @ts-check` 与 JSDoc `/** @type {...} */` 标注类型，以便获得 IDE 类型提示。
- 文档侧边栏通过 `sidebars.js` 中 `autogenerated` 类型按 `docs/` 目录自动构建，无需手动维护条目。
- 页面组件统一通过 `import Layout from '@theme/Layout'` 包裹，并在 `<Layout>` 内设置 `title` 与 `description` 元信息。