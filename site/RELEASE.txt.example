# ============================================================
# 发布新版本文档（与产品版本对应）
#
# 使用方法（共三步，无需任何命令行参数）：
#   1. 把本文件复制一份，重命名为 RELEASE.txt（就在本目录下）
#   2. 把文件最下方的版本号，改成这次要发布的产品版本（如 2.0）
#   3. 重启容器（和平时发布文档一样）：
#        docker compose restart docusaurus
#
# 发布成功后：
#   - RELEASE.txt 会被自动删除
#   - 当前文档（site/docs/）被冻结为该版本，永久保留、随时可访问
#   - 导航栏自动出现版本切换菜单，线上 /docs 继续展示最新文档
#
# 之后修改 site/docs/ 下的文档 = 更新线上最新版（日常发布不受影响）。
# 如需修订历史版本，直接编辑 site/versioned_docs/version-<版本>/ 下的文件。
#
# 注意：
#   - 版本号仅限字母、数字和 . _ - ，例如：1.0、2.5、v3
#   - 若发布失败，本目录会出现 RELEASE.txt.failed，
#     查看容器日志可了解原因（常见：版本号已存在）
# ============================================================
1.0
